#include <stdio.h>
int main()
{
    int i, num[5];
    printf("Enter the five Number:");
    for (i = 0; i < 5; i++)
    {
        scanf("%d", &num[i]);
    }
    for (i = 2; i <=2; i++)
    {
        printf("%5d", num[2]);
    }
}